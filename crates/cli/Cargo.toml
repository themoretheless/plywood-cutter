[package]
name = "cutter-cli"
version = "0.1.0"
edition = "2021"

[dependencies]
cutter-core = { path = "../core" }
cutter-ui = { path = "../ui" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
